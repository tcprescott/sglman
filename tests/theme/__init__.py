"""Tests for presentation components that hold real logic.

Most of ``theme/`` is Vue/Quasar markup that only a browser can exercise (see
the ``/ui-validation`` skill); what lands here is the pure-Python part — layout
maths, row formatting — that is worth unit-testing directly.
"""
